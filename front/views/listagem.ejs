Com certeza! Vamos criar um arquivo .ejs e um .css para exibir a listagem de mensagens que implementamos no seu app.js.

1. Arquivo EJS para a Listagem (views/listagem.ejs)
Este arquivo será responsável por renderizar a tabela com os dados das mensagens. Ele esperará a variável mensagens (que virá do seu app.js quando a rota /listagem-mensagens/:status for acessada).

Crie um novo arquivo chamado listagem.ejs dentro da sua pasta views:

Snippet de código

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listagem de Mensagens - Status: <%= status %></title>
    <link rel="stylesheet" href="/css/listagem.css">
    <style>
        /* Estilos de acessibilidade - pode mover para listagem.css se preferir */
        .accessibility-controls {
            position: fixed;
            top: 10px;
            right: 10px;
            background-color: rgba(255, 255, 255, 0.8);
            padding: 5px 10px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            z-index: 1000;
        }
        .accessibility-controls button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 5px 10px;
            margin: 0 3px;
            border-radius: 3px;
            cursor: pointer;
            font-weight: bold;
            font-size: 1em;
        }
        .accessibility-controls button:hover {
            background-color: #0056b3;
        }
        body.font-size-small { font-size: 0.9em; }
        body.font-size-normal { font-size: 1em; }
        body.font-size-large { font-size: 1.1em; }
        body.font-size-x-large { font-size: 1.2em; }
    </style>
</head>
<body>
    <div class="accessibility-controls">
        <button id="resetFontSize">A</button>
        <button id="decreaseFontSize">A-</button>
        <button id="increaseFontSize">+</button>
    </div>

    <img src="/images/Logo.png" alt="Logo da Simbolus"/><br>

    <% if (messages && messages.length > 0) { %>
        <div class="flash-messages">
            <ul align="center">
            <% messages.forEach(function(msg) { %>
                <li><%= msg %></li>
            <% }); %>
            </ul>
        </div>
    <% } %>

    <div class="page">
        <fieldset>
            <legend>Listagem de Mensagens - Status: <%= status %></legend>

            <div class="client-info">
                <label>Cliente: <%= idCli %> - <%= nome_cliente %></label>
            </div>

            <% if (mensagens && mensagens.length > 0) { %>
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>Código</th>
                                <th>Inclusão</th>
                                <th>Enviar Para</th>
                                <th>Local</th>
                                <th>Data Envio</th>
                                <th>Arquivo</th>
                                <th>ID</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% mensagens.forEach(function(msg) { %>
                                <tr>
                                    <td><%= msg.MWA_CODIGO %></td>
                                    <td><%= msg.MWA_INCLUSAO %></td>
                                    <td><%= msg.MWA_ENVIAR_PARA %></td>
                                    <td><%= msg.MWA_LOCAL %></td>
                                    <td><%= msg.MWA_DATA_ENVIO %></td>
                                    <td><%= msg.MWA_ARQUIVO1 %></td>
                                    <td><%= msg.MWA_ID %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <p class="no-data-message">Nenhuma mensagem encontrada para o status **<%= status %>**.</p>
            <% } %>

            <div class="form-group">
                <a href="/servicos" class="btn btn-secondary">Voltar aos Serviços</a>
            </div>
        </fieldset>
    </div>
    <script src="/js/accessibility.js"></script>
</body>
</html>
