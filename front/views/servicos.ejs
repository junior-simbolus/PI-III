<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opções do Módulo WhatsApp</title>
    <link rel="stylesheet" href="/css/servicos.css">
    <style>
        .accessibility-controls {
            position: fixed; /* Fixa na tela */
            top: 10px;       /* Perto do topo */
            right: 10px;     /* No canto superior direito */
            background-color: rgba(255, 255, 255, 0.8); /* Fundo semi-transparente */
            padding: 5px 10px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            z-index: 1000; /* Garante que fique acima de outros elementos */
        }
        .accessibility-controls button {
            background-color: #007bff; /* Cor azul para os botões */
            color: white;
            border: none;
            padding: 5px 10px;
            margin: 0 3px;
            border-radius: 3px;
            cursor: pointer;
            font-weight: bold;
            font-size: 1em;
        }
        .accessibility-controls button:hover {
            background-color: #0056b3;
        }
        /* Estilo para a classe que será aplicada ao corpo */
        body.font-size-small { font-size: 0.9em; } /* 90% do tamanho padrão */
        body.font-size-normal { font-size: 1em; }   /* 100% do tamanho padrão */
        body.font-size-large { font-size: 1.1em; }  /* 110% do tamanho padrão */
        body.font-size-x-large { font-size: 1.2em; } /* 120% do tamanho padrão */
        /* NOVOS ESTILOS PARA O MENU */
        .menu-options ul {
            list-style: none; /* Remove os marcadores de lista padrão */
            padding: 0;       /* Remove o padding padrão da lista */
            margin: 20px 0;   /* Adiciona margem acima e abaixo do menu */
            text-align: center; /* Centraliza os itens da lista */
        }

        .menu-options li {
            margin-bottom: 10px; /* Espaço entre os itens do menu */
        }

        .menu-options a {
            display: inline-block; /* Permite que o link tenha padding e largura */
            padding: 12px 25px;    /* Padding interno para os botões */
            background-color: #007bff; /* Cor de fundo dos botões */
            color: white;          /* Cor do texto */
            text-decoration: none; /* Remove sublinhado do link */
            border-radius: 5px;    /* Cantos arredondados */
            font-size: 1.1em;      /* Tamanho da fonte */
            font-weight: bold;     /* Negrito */
            transition: background-color 0.3s ease; /* Transição suave na cor de fundo */
            width: 80%; /* Ajuste a largura conforme necessário */
            max-width: 300px; /* Limita a largura máxima */
            box-sizing: border-box; /* Garante que padding não aumente a largura total */
        }

        .menu-options a:hover {
            background-color: #0056b3; /* Cor de fundo ao passar o mouse */
        }
    </style>
</head>

<body>
<div class="accessibility-controls">
        <button id="resetFontSize">A</button>
        <button id="decreaseFontSize">A-</button>
        <button id="increaseFontSize">A+</button>
    </div>
    <% if (messages && messages.length > 0) { %>
        <div class="flash-messages">
            <ul align="center">
            <% messages.forEach(function(msg) { %>
                <li><%= msg %></li>
            <% }); %>
            </ul>
        </div>
    <% } %>
    <img src="/images/Logo.png" alt="Logo do Cliente Simbolus"/><br>
    <div class="page">
        <div class="menu-options"> <ul>
                <li><a href="/mensagem">Enviar Mensagem</a></li>
                <li><a href="/listagem-mensagens/1">Consultar Log</a></li>
                <li><a href="/documentacao">Documentação</a></li>
                <li><a href="/logoff">Sair do Sistema</a></li>
            </ul>
        </div>
        <div class="client-info"> <label>Cliente: <%= idCli %> - <%= nome_cliente %></label></div>
        </div>
      </form>
    </div>
<script src="/js/accessibility.js"></script>
</body>
</html>
